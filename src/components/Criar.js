import React, { Component } from 'react';
import {
  AppRegistry,
  StyleSheet,
  Text,
  View,
  Alert,
  TouchableOpacity
} from 'react-native';

export default class Criar extends Component {
  render() {
    return (
      <View>
        <Text> Criação </Text>           
      </View>
    );
  }
}
